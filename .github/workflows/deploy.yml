name: "Legacy workflow disabled – use nextjs.yml"

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "This workflow is disabled; use nextjs.yml instead."
        required: false
        default: "Use .github/workflows/nextjs.yml for deployments."

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Trigger nextjs.yml for deployments."
